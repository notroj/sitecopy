FROM quay.io/fedora/fedora
RUN dnf -y install httpd-core && dnf -y clean all
COPY httpd.conf /etc/httpd/conf.d/dav.conf
RUN mkdir /var/www/html/dav
RUN chown apache:apache /var/www/html/dav
ENTRYPOINT /usr/sbin/httpd -X -c "ErrorLog /dev/stderr" -c "CustomLog /dev/stdout common"

