
<Directory /var/www/html/dav>
   AllowOverride none
   Require all granted
   DAV On
   # Otherwise OPTIONS on the collection maps to index.html, which is
   # not handled by mod_dav.
   DirectoryIndex disabled
</Directory>
